/**
 * @param {string[]} strs
 * @param {number} m
 * @param {number} n
 * @return {number}
 */
var findMaxForm = function(strs, m, n) {
    // 각 0, 1의 개수로 만들 수 있는 부분집합의 길이
    const dp = Array({length: m}, () => Array(n).fill(0));

    for(let i = 0; i < strs.length; i++) {
        for(let j = 0; j < strs.length; j++) {
            
        }
    }
};